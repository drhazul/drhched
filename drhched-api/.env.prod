# ---------- APP ----------
NODE_ENV=production
PORT=3000
HOST=0.0.0.0

# CORS (usa uno u otro según tu app; deja ambos para compatibilidad)
CORS_ORIGIN=*
CORS_ORIGINS=http://192.168.200.212:3000,http://localhost:5173

# ---------- JWT / AUTH ----------
# Tu esquema (access/refresh)
JWT_ACCESS_SECRET=Cafecitoconpan.2025
JWT_REFRESH_SECRET=Fitsdiario.2025
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Fallbacks comunes (por si la app espera estos nombres)
JWT_SECRET=Cafecitoconpan.2025
JWT_EXPIRES_IN=7d

# ---------- DB ----------
POSTGRES_USER=drhched
POSTGRES_PASSWORD=Siempreseguro.2025
POSTGRES_DB=drhched

# Prisma usará el host del servicio docker "db"
# Nota: 'connection_limit' es opcional; puedes quitarlo si tu pool lo maneja la app.
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}?schema=public&connection_limit=5

# ---------- Prisma ----------
PRISMA_LOG_LEVEL=error

# ---------- Nest (opcional) ----------
GLOBAL_PREFIX=api
HEALTH_ENDPOINT=/health

# ---------- pgAdmin ----------
PGADMIN_DEFAULT_EMAIL=drhazul2013@gmail.com
PGADMIN_DEFAULT_PASSWORD=Soporte.2025
